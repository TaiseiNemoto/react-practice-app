import H1Element from '@/components/common/H1Element';
import ChatSelect from '@/components/lifecycle-of-reactive-effects/Fix reconnecting on every keystroke/ChatSelect';

export default function page() {
  return (
    <main>
      <H1Element title="Fix reconnecting on every keystroke" />
      <ChatSelect />
    </main>
  );
}
